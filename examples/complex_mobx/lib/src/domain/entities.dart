class Product {
  const Product(this.id, this.name, this.price);
  final int id;
  final String name;
  final double price;
}

class User {
  const User(this.username, this.token);
  final String username;
  final String token;
}
